spring:
  application:
    name: alpha
  threads:
    virtual:
      enabled: true
  cloud:
    consul:
      host: 10.37.1.133
      port: 8500
    nacos:
      discovery:
        server-addr: 10.37.1.67
        namespace: dev2
    discovery:
      enabled: true
  webflux:
    base-path: /

server:
  port: 8082
logging:
  level:
    root: trace
